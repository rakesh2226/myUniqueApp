(function() {
    'use strict';

    angular
        .module('app.layout')
        .controller('Sidebar', Sidebar);

    Sidebar.$inject = [
        '$location', '$route'
    ];

    function Sidebar($location, $route) {
      
    }
})();